# llvm bin directory (for llvm-mc and FileCheck toolchain)
LLVM_BIN = "~/git/llvm-project/build/debug/bin"
TOOL_DIR = "../tool"

# assembly code to be translated into machine code & verified by FileCheck
ASM_DIR = "./MC"

# examples
EXP_DIR = "../examples"
